# 🚀 Деплой на сервере

## Быстрый деплой на Ubuntu/Debian сервере

### Автоматический деплой (один скрипт)

```bash
curl -fsSL https://raw.githubusercontent.com/seoshmeo/oura-telegram-reports/master/quick-deploy.sh | bash
```

Скрипт:
1. ✅ Установит Docker и Docker Compose
2. ✅ Клонирует репозиторий
3. ✅ Создаст .env файл (вы введёте токены)
4. ✅ Запустит контейнер
5. ✅ Проверит работу

---

## Ручной деплой (пошагово)

### Шаг 1: Подключитесь к серверу

```bash
ssh user@your-server-ip
```

### Шаг 2: Установите Docker

```bash
# Обновите систему
sudo apt-get update
sudo apt-get upgrade -y

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь для применения изменений
exit
# Подключитесь снова
ssh user@your-server-ip
```

### Шаг 3: Установите Docker Compose

```bash
sudo apt-get install docker-compose-plugin -y
```

### Шаг 4: Клонируйте репозиторий

```bash
cd ~
git clone https://github.com/seoshmeo/oura-telegram-reports.git
cd oura-telegram-reports
```

### Шаг 5: Настройте переменные окружения

```bash
cp .env.example .env
nano .env
```

Заполните:
```env
OURA_TOKEN=ваш_токен_oura
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
TELEGRAM_CHAT_ID=ваш_chat_id
```

Сохраните: `Ctrl+O`, `Enter`, выйдите: `Ctrl+X`

### Шаг 6: Запустите контейнер

```bash
docker-compose up -d
```

### Шаг 7: Проверьте логи

```bash
docker-compose logs -f
```

Вы должны увидеть:
```
Starting Oura Telegram Reports Scheduler
Scheduled daily report at 07:30
Scheduled weekly report on Sundays at 20:00
Scheduled monthly report on day 1 at 20:00
Scheduler is running. Press Ctrl+C to exit.
```

### Шаг 8: Протестируйте отправку

```bash
docker-compose exec oura-reports python oura_telegram_daily.py
```

Проверьте Telegram - должно прийти сообщение!

---

## 🔧 Управление на сервере

### Просмотр статуса

```bash
docker-compose ps
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только последние 100 строк
docker-compose logs --tail=100

# Логи scheduler
tail -f logs/scheduler.log
```

### Перезапуск

```bash
docker-compose restart
```

### Остановка

```bash
docker-compose down
```

### Обновление до новой версии

```bash
git pull
docker-compose up -d --build
```

---

## 📊 Мониторинг

### Проверка работы контейнера

```bash
docker-compose ps
```

Вывод должен быть:
```
NAME                     STATUS          PORTS
oura-telegram-reports    Up 5 minutes
```

### Использование ресурсов

```bash
docker stats oura-telegram-reports
```

### Автозапуск при перезагрузке сервера

Контейнер настроен с `restart: unless-stopped`, поэтому он автоматически запустится после перезагрузки сервера.

Проверка:
```bash
sudo reboot
# Подождите минуту, подключитесь снова
ssh user@your-server-ip
docker-compose ps  # Контейнер должен быть запущен
```

---

## 🔒 Безопасность

### Защита .env файла

```bash
chmod 600 .env
```

### Firewall (UFW)

Если используете UFW, убедитесь что разрешён исходящий трафик:

```bash
sudo ufw status
# Исходящий трафик обычно разрешён по умолчанию
```

### Регулярные обновления

```bash
# Обновление системы
sudo apt-get update && sudo apt-get upgrade -y

# Обновление Docker образов
cd ~/oura-telegram-reports
git pull
docker-compose pull
docker-compose up -d --build
```

---

## 🐛 Решение проблем

### Контейнер не запускается

```bash
# Проверьте логи
docker-compose logs

# Проверьте .env файл
cat .env

# Пересоздайте контейнер
docker-compose down
docker-compose up -d
```

### Не приходят сообщения

```bash
# Проверьте переменные окружения
docker-compose exec oura-reports env | grep -E "(OURA|TELEGRAM)"

# Ручной тест
docker-compose exec oura-reports python oura_telegram_daily.py
```

### Ошибки в логах

```bash
# Посмотрите детальные логи
docker-compose logs -f

# Проверьте scheduler логи
tail -100 logs/scheduler.log
```

### Docker команды не работают

```bash
# Добавьте sudo
sudo docker-compose ps
sudo docker-compose logs

# Или добавьте пользователя в группу docker
sudo usermod -aG docker $USER
# Перелогиньтесь
```

---

## 📍 Расположение файлов на сервере

```
~/oura-telegram-reports/
├── .env                    # Ваши токены (НЕ коммитить!)
├── docker-compose.yml      # Конфигурация Docker
├── Dockerfile              # Образ контейнера
├── logs/                   # Логи (автоматически создаётся)
│   └── scheduler.log
├── oura_telegram_daily.py  # Скрипт ежедневных отчётов
├── oura_telegram_weekly.py # Скрипт еженедельных отчётов
└── scheduler.py            # Планировщик
```

---

## 🔄 Бэкап и восстановление

### Бэкап конфигурации

```bash
# Создайте бэкап .env
cp .env .env.backup

# Или сохраните в безопасное место
cat .env  # Скопируйте содержимое
```

### Восстановление после сбоя

```bash
cd ~/oura-telegram-reports

# Остановите контейнер
docker-compose down

# Восстановите .env из бэкапа
cp .env.backup .env

# Запустите снова
docker-compose up -d
```

---

## 📈 Производительность

### Требования к серверу

**Минимальные:**
- CPU: 1 core
- RAM: 512 MB
- Disk: 1 GB
- Network: постоянный доступ к интернету

**Рекомендуемые:**
- CPU: 1-2 cores
- RAM: 1 GB
- Disk: 2 GB
- Network: стабильное подключение

### Потребление ресурсов

```bash
docker stats oura-telegram-reports
```

Обычно:
- CPU: < 1%
- RAM: ~50-100 MB
- Network: минимальное (несколько запросов в день)

---

## ✅ Чеклист деплоя

- [ ] Docker установлен
- [ ] Docker Compose установлен
- [ ] Репозиторий склонирован
- [ ] .env файл создан и заполнен
- [ ] Контейнер запущен (`docker-compose up -d`)
- [ ] Логи проверены (нет ошибок)
- [ ] Тестовое сообщение отправлено
- [ ] Получено сообщение в Telegram

---

## 🎯 Готово!

Теперь ваш сервер автоматически отправляет отчёты Oura в Telegram каждый день! 🎉

**Следующие шаги:**
1. Дождитесь утра - придёт первый отчёт
2. Проверяйте логи периодически
3. Наслаждайтесь автоматическим анализом здоровья!
